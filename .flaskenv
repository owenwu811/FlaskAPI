#contains variables we want flask when we start it up
FLASK_APP=app2
#start flask with dev enviornment - when we change code, app automatically restarts so we don't have to do it manually 
FLASK_ENV=development
